{
    "version": 2,
    "builds": [
        {
            "src": "api/graphql.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/graphql.ts"
        }
    ]
}