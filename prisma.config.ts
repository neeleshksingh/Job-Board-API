// Prisma v5 doesn't use this file for configuration.
// Configuration is done via:
// 1. .env file for DATABASE_URL
// 2. prisma/schema.prisma for schema definition

// If you need to programmatically access Prisma config,
// you can export values here:

export const prismaConfig = {
  schemaPath: 'prisma/schema.prisma',
  migrationsPath: 'prisma/migrations',
};
